---
title: "Przyczyny stopniowego zmniejszania się długości śledzi oceanicznych wyławianych w Europie"
author: "Piotr Padykuła"
date: "`r Sys.Date()`"
output: html_document
---


```{r globalSettings, echo=FALSE, message=FALSE,include=F}
library(EDAWR)
library(knitr)
require(dplyr)
require(tidyr)
require(ggplot2)
```


```{r loadData,include=FALSE,cache=FALSE}
#Import danych z pliku
df<-read.csv("C:\\MOJE_PLIKI\\Sem3_ZaawEksplDanych\\PROJEKT_2\\sledzie.csv",stringsAsFactors = TRUE)
dataTBL=tbl_df(df)
knitr::opts_chunk$set(echo = TRUE)
```

## Krótkie podsumowanie danych w zbiorze
```{r dataIntroduction, cache=TRUE}
kable(summary(dataTBL))
kable(str(dataTBL))
```

```{r preparingData,include=F}
#data <- tb %>%
    # Wartości pustych nie zamieniamy na zera! Choć można to zrobić jak poniżej
    # replace(is.na(.), 0) %>%
    # Kolumny child, adult, elderly, zamienione na zmienną nominalną
    #gather(age, cases, child:elderly)
```